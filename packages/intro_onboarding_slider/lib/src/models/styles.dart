/// Shared enums and style types for intro_onboarding_slider.
library styles;

/// Layout styles for onboarding pages.
enum IoLayoutStyle { breathable, compact, showcase }

/// Final button visual styles.
enum IoLastButtonStyle { large, smallRounded }
