name: guardian_onboarding
description: Reusable, customizable onboarding flow for Flutter apps with pages, indicators, and navigation controls.
version: 0.1.0
homepage: https://example.com/guardian_onboarding
repository: https://example.com/guardian_onboarding
issue_tracker: https://example.com/guardian_onboarding/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  flutter_animate: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
